{
  "buildCommand": "flutter pub get && flutter build web --release --dart-define=API_BASE_URL=$API_BASE_URL --base-href=/",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
