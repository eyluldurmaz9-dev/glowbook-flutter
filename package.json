{
  "name": "glowbook-flutter-web",
  "private": true,
  "version": "1.0.0",
  "description": "Vercel deployment wrapper for the GlowBook Flutter Web app.",
  "scripts": {
    "vercel-build": "bash scripts/vercel-build.sh",
    "build:web": "flutter build web --release --dart-define=API_BASE_URL=$API_BASE_URL --base-href=/"
  },
  "engines": {
    "node": ">=20"
  }
}
