class AppConstants {
  AppConstants._();

  static const String appName = 'GlowBook';
  static const String defaultLocale = 'tr_TR';
  static const Duration connectTimeout = Duration(seconds: 45);
  static const Duration receiveTimeout = Duration(seconds: 75);
  static const Duration sendTimeout = Duration(seconds: 45);
}
